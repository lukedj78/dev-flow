# Changelog

All notable changes to the dev-flow skill suite. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versioning is [semver](https://semver.org/) on the **suite as a whole** (the version in `.claude-plugin/plugin.json`).

What a bump means here:
- **major** — a breaking change to the `.workflow/` contract (phase enum, `meta.json` schema, a skill's inputs/outputs) that existing projects must migrate for.
- **minor** — new skills, new capabilities, new doc-grounded references, non-breaking contract additions.
- **patch** — fixes to existing guidance, upstream re-verification, docs.

## [Unreleased]

### Fixed
- **RN/Expo stack-defaults: `react-native` patch drift, and `react-native-reanimated`/`react-native-worklets` bumped as a verified pair.** Extended ecosystem-watch pass on request, past the usual Vercel/eve/shadcn set. `react-native` was documented as `0.86.2`; SDK 57's own template (`expo-template-blank-typescript@57.0.26`) pins `0.86.3` — fixed in both `rn-bootstrap/references/stack-defaults.md` and `rn-fundamentals/references/stack-defaults.md`. `react-native-reanimated` `4.5.1` → **`4.7.0`**, with `react-native-worklets` `0.10.1` → **`0.13.0`** to match (Reanimated 4.7.0 requires worklets 0.13.x) — checked against Reanimated's own peer range (`react-native 0.86–0.88`), which SDK 57's `0.86.3` still satisfies, so this is a safe bump, not a forced one. **Deliberately not applied**: `scripts/refresh_stack_defaults.py`'s dry run flags 10 more "drifted" packages, but its own documented caveat is that npm `latest` is the wrong authority for Expo-bundled native modules (its real source, `unpkg.com`'s `bundledNativeModules.json`, is egress-blocked from this session) — e.g. it suggests `react-native-gesture-handler` 2.32→3.3, a whole major `expo install` would never actually apply to an SDK 57 project. Left alone rather than bumped on the wrong authority.
  - Also checked: **React 19.3.0** shipped stable 2026-09-09 — RN/Expo projects stay correctly on React 19.2.3 (SDK-bundled), so no change there; `transitions/references/motion-library.md`'s framing of 19.3 as a "future rung" is unaffected. `@base-ui/react` 1.7.0 → 1.8.0 verified clean (identical primitive set between the two tarballs).
  - ⚠️ **Flagged, not fixed**: **Vitest 4 → 5.0.1** (major, 2026-09-03) — `vitest.dev`, GitHub and `unpkg` are all unreachable from this session, so the migration guide couldn't be read. Confirmed from the shipped package alone: `engines.node` dropped Node 20 (now `^22.12 || ^24 || >=26`), `vite` peer moved to `^6.4 || ^7 || ^8`. `module-add/references/module-test.md` doesn't pin a Vitest version, so no doc currently contradicts v5, but the 2026-08-04 "Vitest 4 verified-clean" log line is stale and nobody has read v5's actual migration notes yet — revisit once one of those sites is reachable.
- **`eve-agent`: `autoModel`, `t.judge.autoevals` and `agent/instrumentation.ts` — all three renamed or removed within days of being documented.** Scheduled ecosystem-watch pass, eve 0.58.1 → 0.63.0 (five minors, five days). `autoModel` (`eve/experimental/evaluate`), written up from the 2026-09-17 pass, was renamed **`auto`** and moved to **`eve/models`** in 0.60.0 (the standalone `evaluate` moved to `eve/ai`); the old subpath no longer resolves. `t.judge.autoevals.*` was removed in 0.62.0 — `t.judge(...)` is now called directly, itself wrapping `evaluate`. `agent/instrumentation.ts` no longer builds as of 0.62.0: `agent/instrumentation/` (one file per slot) is the only supported layout, the `experimental.instrumentationProviders` flag no longer exists, and the OTel destination `exportPolicy` shape changed underneath it in 0.60.0 (`redactSpanInputs`/`redactSpanOutputs`/destination `recordInputs`/`recordOutputs`/`content`/`composeSpanExportPolicies` removed, now throw). Every claim corrected against the shipped `docs/`, not the changelog bullets: `eve-concepts.md`, `eve-scaffold.md`, `eve-patterns.md` §13, `eve-evals.md`, `eve-capabilities.md`, `eve-conventions.md` §Observability, `SKILL.md`, `eve-docs-coverage.md`, `README.md`, `eu-data-sovereignty.md` §4.10. Also noted for next pass: `t.newSession()` → `await t.session()` (0.59.0). See `docs/vercel-changelog-watch.md`'s 2026-09-22 row for the full account.
- **Three skill bugs found scaffolding FITROOM** (Next 16.3.4, shadcn 4.21 base-vega, `@tanstack/react-form` 1.33.5, better-auth 1.7.5, drizzle-orm 0.45.3, PGlite 0.5.8).
  - `design-md-to-app`: `no-restricted-imports` patterns are gitignore-style, so a bare `"input-otp"` also banned the local wrapper `@/components/ui/input-otp`. Slash-free names are now written as `"/input-otp"`. Verified with ESLint 10: the wrapper passes, the package stays banned.
  - `forms`: the TanStack toolkit now typechecks. It uses `AnyReactFormApi` (built on `ReactFormExtendedApi`) instead of `AnyFormApi`, which has no `Field`/`Subscribe`. The `never` casts are gone, the schema is typed on its input, and `AbortController` lives in a ref. The toast follows the installed primitive: Base UI `toast.add` or sonner `toast.error`. Also fixed the `SyntaxWarning` in `scaffold_lib_forms.py`.
  - `module-add`: `module-db` now documents the lazy client (`getDb()` + `Proxy`; an eager one aborts `next build` under PGlite). The neon-serverless `Pool` is the default, because neon-http has no interactive transactions. dotenv reads `.env.local`. `module-auth` now recommends email OTP for PWAs and documents the Next 16 `cacheComponents` pattern (`proxy.ts` cookie check, then `'use cache: private'` `getCurrentUser()` inside `<Suspense>`). `drizzleAdapter` takes the lazy `db`.

### Changed
- **Expo SDK 58 is in beta, and our beta page said the opposite of what `expo@next` now does.**
  `rn-upgrade/references/beta-preview.md` was snapshotted on 2026-08-26, when `next` pointed at stable
  `57.0.16`, and told the reader that `expo install expo@next` installs stable. On 2026-09-22 `next` is
  **`58.0.0-preview.5`**, so the same command moves a project onto a beta. The page now carries the
  current dist-tag map (`latest` `57.0.24`, `canary` `58.0.0-canary-20260909-ea7a89a`), states the trap in
  both directions, and says to name the version rather than the tag. Same correction in `rn-upgrade`
  Step 8.
- **`rn-upgrade/references/breaking-changes.md` §SDK 58 — the crossing, written before we have to make it.**
  From the official beta changelog (2026-09-15): iOS 27's **UIScene life cycle** (`prebuild` generates
  `SceneDelegate.swift`; bare and hand-edited AppDelegates need the migration) and **resizable apps**, where
  `ScreenOrientation.lockAsync` may do nothing and `requireFullScreen` no longer opts out; React Native's
  **Strict TypeScript API** as the default, with the `react-native-legacy-deep-imports` escape hatch removed
  after 0.88; the removals (`InteractionManager`, `Modal`'s `animated`, `StatusBar` props, `ImageBackground`
  deprecated); the **expo-router core rework**; `File.write()` going async; libSQL dropped from `expo-sqlite`;
  foreground notifications shown by default; **R8 on by default** on Android; and Expo CLI now setting
  `NODE_ENV` itself, so `NODE_ENV=test npx expo start` loads `.env.development`. The beta's own cost is
  recorded too: EAS Build images with Xcode 27 were still "coming soon", Expo Go 58 is not in the stores, and
  React Native 0.88 is an RC. `expo@57` stays the pinned default.
- **`rn-components-apis` rule 4** now says an orientation lock is not a layout guarantee on iOS 27, next to
  the existing `useWindowDimensions()` rule.
- **Noted, not adopted:** `@expo/agent-cli` (MIT, experimental) with `smoke` = start + screenshot + stop and
  `skills:sync` for agent skills shipped in `node_modules`, and `expo-device-hub` now an official dev-tools
  plugin. Both are candidates for the visual-verification loop the `rn-*` skills lack; revisit at SDK 58 stable.
- **`eve-agent` §13 / `eu-data-sovereignty.md` §4.10: where Jev runs, stated precisely, and Laya as the EU-hostable
  option.**
  - **Jev.** "Jev is a US transfer" was too blunt. The Gateway lists exactly one Jev endpoint, TypeSafe's, with no
    region. TypeSafe hosts in the US, and the region our own code runs in does not change that. So it is a transfer
    **when the `state` holds personal data**, and minimising the state is the control. An EU pin through the
    Gateway's `inferenceRegion` is documented to fail closed (400) without an EU endpoint: one call answers it, not
    yet made.
  - **Laya** (`NandhaKishorM/laya`, Apache-2.0 with open weights, lists Italian) is the same kind of typed-decision
    model, one you can run on your own EU machines. It is written up as **to measure, not a default**: four days old,
    512–1,024 tokens of context, weaker than Jev on the independent numbers, confident while wrong on non-Latin
    script, and Python only. Found through madewithlaya.com, an unaffiliated directory.
- **`eve-agent` §13 (evaluation models): confidence, a mock for the branches, the context limits.** Three things
  Vercel's KB guide *"How to classify, route, and score with Jev and AI SDK"* (2026-09-19) documents and §13 did not.
  - `providerMetadata.typesafe.confidence`: distribution concentration, only for `choice`/`score`, untyped, and
    gated together with the selected probability, with missing metadata failing closed.
  - **`Experimental_EvaluationMockModelV4`** from `ai/test` (verified in `ai@7.0.109`): it unit-tests the
    thresholds with no network or Gateway credentials, while the eval in (d) keeps the calibration. New
    subsection (f), with the four cases to cover.
  - **64k tokens per request, 32k for the `state`**, and a `score` that is a 0-indexed probability-weighted mean.

  The boolean-has-no-confidence trap is from Langfuse's *"Using TypeSafe's Jev for evals"* (2026-09-18). Found
  through shipwithjev.com, an unaffiliated directory of Jev builds that is not itself a source.
- **shadcn's registry now imports `cn` from the `cn` package, and dev-flow says which way to point it.** The shadcn changelog ("September 2026 - cn") moved every registry component, block and example to `import { cn } from "cn"`. Each item now declares `cn`, `init` writes a one-line `lib/utils.ts` re-export, and existing projects are told *"Nothing breaks"*. We had made `cn` the default on 2026-09-02 behind `@/lib/utils`, so what changed is where primitives import it from. The compliance runs showed agents reading the new import as a broken install and rewriting vendored primitives back. `design-md-to-app/references/shadcn-mapping.md` §cn now gives two opposite rules, depending on `lib/utils.ts`:
  - **Stock:** leave `from "cn"` alone. It is the same function, and rewriting it forks a primitive.
  - **Configured** (`createCn` / `extendTailwindMerge`, as in bottega and video, for a custom type scale): every `cn` must come from `@/lib/utils`, primitives included. The package's stock instance would bring back the colour silently dropped by `cn("text-pure-white", "text-body")`.

  `setup_design_lint.py` enforces the configured case. It detects the config and bans `import { cn } from "cn"` everywhere but the utils file, `components/ui/**` included, in a separate flat-config block so the golden-rule-3 bans are not replaced. Checked on ESLint 9.39.4: `cn/config` and `{ clsx } from "cn"` stay allowed. There are 27 tests.

  Also written down: never two mergers in one workspace. bidmaster and eve-hospitality `apps/web` are still on clsx + tailwind-merge next to a `cn` `packages/ui`, and desko was never migrated.
- **`registry-intake`: a reused npm name is D8, and D5 dates the current owner's line.** `npm view cn time.created` says 2013, because the name was a Chuck Norris joke package (rumpl/cn, 0.1.x) before shadcn's 0.2.0 on 2026-09-01. D5 read that date and would have waved a three-week-old package through. D8 flags a reused name, and a long gap alone is not enough (clsx paused 2020 → 2022 with the same repo and maintainer). It takes two years between releases **and** a different repository **and** no maintainer in common. `motion` turned out to be one too: 5.0.0-beta was steelbrain/pundle. Both owners were checked on 2026-09-22 and recorded as `info`. D7 now knows that under 1.0 a caret locks the minor (`cn@^0.2.4` refuses 0.3.x). Items from shadcn's own registry depending on `cn` are expected and still not governed. 30 tests (+3).
- **Re-measured after the ask rule** (`evals/compliance/baselines/2026-09-22/registry-intake-after-ask-rule.md`, 9 sessions, 4.48 USD): every session, at every level including time pressure, stopped after the review and asked for both decisions. Before, one in three did.
- **`registry-intake`: the agent always stops and asks before `allow` and `approve`.** The first compliance baseline (`evals/compliance/baselines/2026-09-22/`) showed that the old wording, *"a new registry is the user's decision"*, with nothing said about approving a clean item, was followed in one session out of three. In the other two the agent allowlisted the registry and approved the item itself, putting the operator's login in `--by`. The workflow now makes both commands the user's decisions at every tier and under any prompt pressure. `--by` is the person who said yes in the conversation, never the agent or the OS login, and an autonomous run stops at the review with *"needs a human"*. The same text is in the AGENTS.md section `setup` writes. **Backed mechanically**: the PreToolUse hook answers `"ask"` (documented alongside `allow` / `deny` / `defer`) on `registry_intake.py allow|approve`, with a prompt that names the registry or item and the `--by`, so an agent that skipped the question still meets the user. `deny` keeps precedence. 27 tests (+4).
  - The compliance gate follows the rule. `agent_decided` (running `allow`/`approve` in a session nobody can answer) is now a scored violation, and a new `stopped_to_ask` outcome (builtin `final_asks`) passes when the session ends on a question that names the decision. The generic "halt" relaxation it replaces is removed.

### Added
- **`module-add email`: templates from emailcn through registry-intake, always React Email with `<Tailwind>`, and what a first real send taught.** On Hostitaly an agent wrote the auth template with inline styles and exempted `emails/**` from the design lint to make it pass — a silent deviation from this reference, caught by the user. Now written down: styles go through `<Tailwind>` (it inlines at render: 16 `style=` and no `class=` in the output), a lint conflict in email templates means *stop and ask*, and **emailcn comes first** — namespace `@emailcn` at `https://emailcn.run/r/{name}.json`, reviewed and installed through `registry-intake`. The item table is read from `registry.json` because the site lists neither names nor URL: auth magic link, OTP, password reset, invite, themes and fonts; **no email-verification block** (derive one from the magic-link block). What an install brings and must change: files in `components/email/`, a theme object for `createEmailTailwindConfig` to author from DESIGN.md (never `theme-default` or a brand-lookalike), hard-coded English copy to turn into i18n props, and `email-assets.ts` pointing at Unsplash/icons8/picsum — never send remote images you do not own (open tracking). Also: Resend's test sender reaches only the account owner and `delivered@resend.dev` / `bounced@resend.dev` prove the API path; better-auth emails take their locale from the link's `callbackURL`, fail closed when deployed without a key and never put the link or address in errors; the dev redirect is gated on `VERCEL_ENV`, not `NODE_ENV` (previews run production builds); webhook `verify` wants the three Svix headers as an object and the SDK constructor needs the API key even to verify; in layered projects render in the caller and pass `html`/`text` through the port. Checked on `resend@6.28.1`, `react-email@6.9.5`.
- **`evals/compliance/` — does an agent follow the gates when nobody reminds it?** Every gate has a mechanical half with unit tests and a half that is a sentence (*search `components/ui/` first*, *review before approve*, *register the provider*). The sentence was never measured, and the scripts cannot see it: `registry_intake.py check` passes after a `shadcn add <url>` that bypassed it, and `data_residency.py check` passes on a register missing the provider just wired in. `comply.py run <gate>` copies a fixture dev-flow project, prepares it with the real scripts, runs `claude -p` with the installed skills at three levels (supportive / neutral / competing), and grades the trace and final tree **deterministically**. Steps are regexes over tool calls: scoped to an input key, ordered by call and by position inside one call, `forbid` with `unless_after`. Outcomes are the gate's own `check` or builtins that read the design-lint preset's own lists, and a stop to ask a human counts as compliance. The idea is ECC's `skill-comply` (MIT); the LLM spec generator and classifier are dropped, because our gates are named scripts and named paths. Three gates: `golden-rule-3`, `registry-intake`, `data-residency`. `selftest` has 13 hand-written cases, with every check failing in one and passing in another, runs in CI and is offline. `regrade` re-scores kept runs for free after a grader fix.
  - **First baseline** (`baselines/2026-09-22/`, Sonnet, 9 sessions, 10.45 USD): 100 % at every level on all three. What the scores do not show is that registry-intake never says whose decision a new registry is. At level 2 the agent stopped and asked; at levels 1 and 3 it allowlisted and approved with `--by` set to the operator's login, recording a human approval that never happened. Also found: shadcn's registry now imports `cn` from its own new `cn` package, and agents "repaired" the vendored primitives back to `@/lib/utils` (spun off).
- **React Bits integrated as a motion source, not a component library.** `transitions/references/react-bits.md`: 202 animated components served as a shadcn registry (`@react-bits`, items at `https://reactbits.dev/r/{name}.json`, 4 variants per component — always `TS-TW`), licence **MIT + Commons Clause**, so usable inside a product but *"never sold, sublicensed or redistributed as components"* — nothing of it is ever vendored into this repo or a published `packages/ui`. The `/c/micro` set (30 items, listed) is Motion-based and calls `useReducedMotion()` itself. The load-bearing part is the **primitive map**: Spring Check → `Checkbox`, Squish Switch → `Switch`, Jelly Radio → `RadioGroup`, Swipe Toast → `Sonner`, four buttons → `Button`, four sliders → `Slider`, Code Slots → `InputOTP` … so golden rule 3 makes the default action "port the timings and gestures onto our primitive through `lib/motion/tokens.ts`", and only the eleven with no primitive behind them are installs — through `registry-intake`. Its other categories: GSAP items would add a second animation engine (and GSAP's is a *"Standard 'no charge' license"*, not OSI); `ogl`/`three` backgrounds are Tier 4 and go through `vgpu-shaders`' gating questions.
- **Two checks the run against that registry earned** (`registry-intake`, 23 tests): **D7** — an item's dependency range that excludes the major the project has installed (every React Bits micro item asks `motion@^12.23.12`; our projects run 13.4.0), read from `node_modules` or the manifest; and **S6** — a `.tsx` line over 1000 characters, which the old S2 called minified code and *blocked*. `SwipeToast` has one 1070-character Tailwind class list of arbitrary values: not obfuscation, but a real cost, since these files land outside the exempt `components/ui/` and the design-lint cap cannot be raised to fit them. S2 now requires packed statements (≥ 3 `;` on the long line) before it blocks.
- **Jev, analysed: where it runs, what it costs, what it is not.** TypeSafe AI's privacy policy (read 2026-09-17) says the service is *hosted in the United States* and names no DPA, SCCs or DPF, so `eu-data-sovereignty.md` §4.10 now records it as a US transfer instead of "EU processing not verified", with the Gateway catalog's `zdr: all` / `no_training: all`. `eve-patterns.md` §13 gains the cost reality — $0.042 per million input tokens, output free, and a per-100k-decision comparison at the Gateway catalog's prices ($1.70 vs $3.20 for `gpt-5-nano`, $55 for Haiku 4.5, $110 for Sonnet 5): the 444.6× headline is against frontier models in non-reasoning mode, and against small models the gap is about 2×, so the case for it is the typed, probability-carrying answer and the latency. Also recorded: Italian is not documented, text-only input, `choice` ≤ 255 options, no published calibration figures.
- **`eve-agent` §13 — evaluation models (Jev) used where they earn their place.** Each hook point checked against eve@0.58.1's bundled docs before being written: **(b)** a risk-scored `approval` policy — the policy may return a promise, so `experimental_evaluate` can decide `"user-approval"` vs `"not-applicable"`, failing closed and never auto-`"approved"` on anything irreversible; **(c)** a guardrail **inside the tool**, not in a hook, because eve's hooks are *"observe-only"* — added to §11's fence, never instead of it, and the cheap first signal for special-category data (R9, memory screening); **(d)** evals — not as the judge (the judge is a `LanguageModel`), but inside `test(t)` graded with `t.check(value, satisfies(…))`, with the two differences that bite: `evaluate` throws without Gateway credentials where a judge skips, and `satisfies` is a gate; **(e)** the cheap pre-filter in front of §12's investigation. And one it does **not** fit, stated so nobody builds it: choosing the subagent — the parent model picks by `description`, and subagent resolvers do not run at `step.started`. Every use: a processor to register, thresholds calibrated with evals, questions designed like a form, experimental on both sides, cost measured on your own traffic. Signature and result read off `ai@7.0.105`'s `index.d.ts`.
- **`eve-agent`: `autoModel` and the evaluation modality.** AI Gateway now serves **evaluation models** (TypeSafe AI's Jev, changelog 2026-09-16): `experimental_evaluate` in `ai` ≥ 7.0.105 returns typed `boolean` / `choice` / `score` answers with probabilities instead of text — documented in `eve-scaffold.md` next to service tiers, with its limits (AI SDK only, not the compatible endpoints; per-token billing; ZDR supported) and the vendor's speed claim marked as the vendor's. eve@0.58.1 builds on it: **`autoModel`** from `eve/experimental/evaluate` picks the agent's model per turn from an allowlist of described options — written up in `eve-concepts.md` §Agent config from the bundled `docs/guides/evaluate.md` (evaluates at the first `step.started`, reused within the turn; the evaluator sees option keys, descriptions and ≤ 8 recent messages capped at 16,000 characters; requests without user text fail before provider I/O). Added what the page leaves to us: it is experimental, it costs one evaluation call per turn, and the evaluator is a **processor that sees conversation text** — recorded through `data_residency.py add` under an EU residency decision. Coverage map and ecosystem watch updated.
- **`transitions`: Motion's `<AnimateView>`, and the version gate that makes it fail in the browser rather than at install.** `motion@13.4.0` ships `motion/react-animate-view` — the declarative twin of the imperative `animateView` already documented on the Tier-2/Tier-3 seam: `name`, `transition` and the four slots `enter` / `exit` / `share` / `update`, read off `framer-motion@13.4.0`'s `animate-view.d.ts`. It renders React's `<ViewTransition>` with `onEnter`/`onExit`/`onShare`/`onUpdate` wired to Motion, so the browser still runs the transition. Its source throws *"AnimateView requires React 19.3 or later."* when `React.ViewTransition` is missing, while `motion`'s `peerDependencies` still read `^18 || ^19` — so it installs on our React **19.2.4** projects and breaks at runtime. Also corrected: `ViewTransition` **graduated in react@19.3.0** (`exports.ViewTransition` in that package's `cjs/react.production.js`), so calling it React's experimental `unstable_ViewTransition` is now only true for projects below 19.3. Documented with it: not interruptible, a duplicate `name` fails the animation, page-level not micro-interactions, and — as with vgpu — the docs never mention `prefers-reduced-motion`, so non-negotiable 2 is ours to honour.
- **Where the data lives is decided before the stack is wired — and a provider without an EU option is flagged, never a blocker.** Planning a project that handles identity documents, tax data and filings to authorities showed the gap: residency was decided service by service, and sub-processors and US transfers surfaced afterwards, while `compliance-audit` only checks at `feature_complete` — when a Blob store's, a Neon project's or a Linear workspace's region can no longer change.
  - **`dev-flow/references/eu-data-sovereignty.md`**: the four questions (EU data subjects, sensitive categories, sector residency, CLOUD Act), what `eu` / `eu-sovereign` / `none` mean (region vs. corporate control; SecNumCloud is per offer, never per company), seven operating rules (explicit EU regions, a register fed by every module, transfer basis with the DPF appeal C-703/25 P pending, applicative deletion because lifecycle rules work in days, KEK out of env vars, AI with EU processing and no training, payment providers as independent controllers), a **service table for 13 categories** — hosting, database, object storage, backup, CDN, email, auth, payments, analytics, observability, AI/LLM, queues, key management, mobile and project tooling — each with the default, its EU setting and European alternatives, every row sourced on the vendor's own page on 2026-09-15, and a **regulation table** with primary links and re-verification cadence (GDPR, DPF, SCCs, EDPB incl. Opinion 22/2024, AI Act as amended by Regulation (EU) 2026/1744 — Annex III high-risk moved to 2 Dec 2027, Art. 50 from 2 Aug 2026 — the Data Omnibus as a proposal only, NIS2 and D.lgs. 138/2024, Data Act, eIDAS 2, CLOUD Act, Codice privacy, TULPS art. 109 with the Alloggiati Web deletion rule and the Garante note of 29 Apr 2026 on guests' ID documents).
  - Found in the research and now written down: Anthropic's own API has no EU inference (use Vertex `eu`, a Bedrock EU profile or Foundry); the AI Gateway's EU pin does not pin the gateway hop; Resend's EU region moves sending, not storage; Neon (now Databricks) has one EU region; Supabase's generic "Europe" includes London and Zurich; Firebase Auth runs only in the US; Vercel Workflow 4.x runs always live in `iad1`; Trigger.dev's EU region is execution, its DPA stores data in US-East-1.
  - **`dev-flow/scripts/data_residency.py`** (`decide` · `add` · `check` · `render`): the decision in `stack.data_residency` / `compliance.data_categories`, the register in `compliance.sub_processors` rendered to `docs/compliance/subprocessors.md` (one register, customer-facing), and a check of register vs. declared stack and of regions in `vercel.json` and `.env*.example`. A non-EU provider is recorded with its flags and the command exits 0. 9 tests, in CI.
  - **Gate**: `update_meta.py set-phase scaffolded` refuses an undecided residency — nothing else. `show_state.py` shows flags without blocking.
  - **Wired**: dev-flow §Stack decisions asks the questions and proposes EU region and European alternative per service; `module-add` Step 1b and `rn-module-add` Step 2b set the EU region and record every provider; `eve-agent` covers model route, Sandbox region and payload minimisation; `compliance-audit` audits R3/R8/R9 against the recorded decision; `vercel-doctor` surfaces non-EU regions. Contract: `stack.data_residency`, the `compliance` field, a default-libraries row (39 copies).
- **Golden rule 3 — UI is composed only from the chosen library's primitives.** The rule existed in one place, `design-md-to-app` Step 4.3, so every skill that writes UI after the scaffold was free to forget it and no check looked for it; planning a project (Hostitaly) showed the gap. It is now in the contract next to "code in English" and "i18n from day one", with its operative step — **before writing a component, search `components/ui/` (or the mobile equivalent) for the primitive; if it is there, use it** — and a defined exception record (`meta.json#stack_config.primitive_exceptions` or an ADR, always with the reason). `scaffolded` is written down as the phase where every primitive is installed (`shadcn add --all`), so the rule is always applicable.
  - **Every UI-producing skill cites it**: `design-md-to-app` (Step 4.3 now points at it), `screenshot-to-page` (its "hand-roll a small custom component" bullet, which contradicted the rule, is gone), `forms`, `module-add`, `transitions`, `coss-ui`, `composition-patterns-guide` (a new rule zero), `rn-add-screen`, `rn-module-add`.
  - **Checked three ways**: the design-system lint preset gains `no-restricted-imports` for undeclared component libraries (MUI, Chakra, antd, Mantine, Headless UI, HeroUI/NextUI, …) and for the primitives' headless bases (`radix-ui`, `@radix-ui/react-*`, `@base-ui/react`, `cmdk`, `vaul`, `input-otp`) outside `components/ui/` — every package name verified on npm, measured at zero hits across nine projects before being turned on, and exercised on gym-saas (three probe imports reported, `sonner` untouched); `spec-review`'s Standards axis checks the diff for re-implemented primitives, undeclared libraries and forked `components/ui/` behaviour, with an example finding; `shadscan` routes "a component that duplicates a primitive" to the rule and the owning skill instead of patching the copy.
  - **Contradictions removed**: reference snippets that rendered raw `<button>`/`<input>` where a primitive exists now use `Button`/`Input`/`Tabs` (`state-discipline`, `data-fetching`, `module-add` realtime, `i18n-next-intl`, `chat-and-typeset`, `animated-icons`); standalone Base UI's `Button` is the primitive, not a raw element. Found on the way: Base UI's package was renamed `@base-ui-components/react` → `@base-ui/react` (npm marks the old one deprecated) and four references still used the old name.
- **A pre-commit hook that runs the generators.** `.githooks/pre-commit` (enable once with `./scripts/install-git-hooks.sh`) fires only when a `SKILL.md`, a `references/` or a `scripts/` file is in the commit, runs `scripts/regenerate.sh` — `skills.json` → `docs/` site → skill-map row meta → both plugin manifests, in dependency order — stages the results and lints. Four PRs in a row went red in CI on exactly this (stale `skills.json`, a stale site page, drifted skill-map counts) and every stacked branch then conflicted on `skills.json`; the fix belongs at commit time, not review time. New `scripts/sync_skill_map.py` is the writer check 13 never had: it rewrites each `1122·9r·2s` row from `skills.json` (same regex as the check), so the map cannot disagree with the registry. `SKIP_REGEN=1` or `--no-verify` bypasses the hook for one commit; `REGEN_BUNDLES=1` opts into repackaging `dist/*.skill`.
- **Autonomous runs — what a skill does when nobody can answer.** `dev-flow` §Autonomous runs and `prd-from-idea` Step 3 §Autonomous runs: in a background / `claude -p` / batch session a blocking question stalls the pipeline with nothing delivered. The rule is now written down — **derive** from the brief before **defaulting**, default only where the contract names one, **write every derived or defaulted answer down as an assumption** (`PRD.md` §Open questions as `Assumed: … — because …`, `history[].inputs.assumptions`), and **never derive** the topology shape, anything outward-facing or hard to undo (deploy, store submission, paid services, deletion), or credentials — those stop the run with *"needs a human: …"* while everything else finishes. Detection is explicit, not inferred: the user said so, or the harness is non-interactive; when in doubt it is interactive. Found by rebuilding a project from a transcript with no user in the loop: the pipeline worked, but the skills had no words for what it did.
- **`cms` module — Sanity as the content layer, the Studio as the admin panel.** `module-add cms` (web, `references/module-cms.md`: `next-sanity` 13 with `defineLive` / `sanityFetch` in Server Components, `server-only` read token, `<SanityLive />` in the layout) and `rn-module-add cms` (mobile, `references/module-cms-sanity.md`: the app reads over Sanity's **HTTP Query API with plain `fetch`** — no SDK in the bundle, nothing for Metro to mis-resolve; the Studio lives in a `cms/` package excluded from the Metro graph; seed by `@sanity/client` transaction with deterministic `_id`s). Both are grounded in the Sanity docs (query URL shape, the **11 KB GET cap**, the **static `apiVersion` date** rule, public vs private datasets, *"never add an access token to JavaScript that is bundled for client-side use"*) and in `sanity-io/sanity-template-nextjs-clean`. Contract: `stack.cms` (`"sanity"` default when the product needs one), a `cms/` line in the folder layout, a **Content / headless CMS** row in the recommended defaults; `dev-flow` asks once at stack-decision time, next to the agent-engine question.

  The motivating case is a tutorial that built a flight-booking app with Sanity Studio as its admin and Supabase for bookings — and never wrote an admin screen or an admin role. The split it got right is now the rule: **content a non-developer edits and the app only reads → CMS; user-owned data → `db` behind RLS.** The same tutorial lost three prompt rounds to a GROQ query that compared a field on a `reference` instead of dereferencing it — the `->` rule is in both how-tos.
- **`rn-write-tests` §Two-user flows (shared state).** A Maestro template for the test that a single-user flow cannot express: user A takes a seat / slot / invite, signs out, user B signs in and must see it taken. Two `runFlow` calls into a shared `common/sign-in-as.yaml` with per-user `env`, `clearState: true` between users (the mobile mirror of `rn-backend` rule 5 — sign-out clears everything), assertions on `testID` + state selectors (`enabled: false`) rather than copy, and the unique-resource-per-run rule. This is the check that surfaced the cross-user RLS bug in that same tutorial, and nothing in the kit proposed it.

### Fixed
- **`rn-bootstrap` scaffolded an app that did not start.** `init-expo-app.sh` installed `expo-router` alone, and under `--legacy-peer-deps` npm does not install peers — so `expo-linking`, `expo-constants` and `react-native-screens` (all three declared by `expo-router@57` as peerDependencies) were missing. `tsc --noEmit` passed; the first `expo start` failed with *Unable to resolve "expo-linking"*. The script now installs the three peers with `expo install`, and `verify.ts` (a) checks them and (b) runs **`npx expo export --platform ios`** into a temp dir — the one check that exercises Metro's resolver, which no amount of type-checking does (`RN_BOOTSTRAP_SKIP_BUNDLE=1` to skip while iterating). The checklist also adopts the rule the tutorial this was found against gets right: **open the empty app once before adding anything**, so there is a known-good baseline to debug from. Found rebuilding a flight-booking app on SDK 57 / expo-router 57.0.20, 2026-09-08.
- **`rn-write-tests`: every suite failed before running.** `jest-expo@57` declares `@react-native/jest-preset ^0.86.3` as a peer; the package is not SDK-pinned, so `npx expo install` hands you npm latest — `0.87.1` on 2026-09-08 — whose `jest/setup.js` mocks `react-native/setup-env`, a file that exists only from RN 0.87. The install snippet (SKILL.md and `references/jest-setup.md`) now reads `react-native/package.json` and pins the preset to that exact version.
- **TypeScript 6 in `rn-bootstrap`.** The SDK 57 `blank-typescript` template ships TS 6, which (a) reports **TS2882** on side-effect imports without a declaration — `import "../global.css"` and the three `@formatjs/intl-pluralrules` polyfill imports `i18n-rn.md` prescribes — and (b) makes `baseUrl` a hard error (**TS5101**). `wire-nativewind.ts` now writes a root `declarations.d.ts` (idempotent — never overwritten), `verify.ts` checks for it, step 5 says `paths` without `baseUrl`, and `i18n-rn.md` documents the error and the file.
- **`DESIGN.md` was read from the wrong place.** The contract puts it in `.workflow/`; `wire-nativewind.ts` read `<root>/DESIGN.md`, so a project that followed the contract silently got the default tokens. Contract path first; the root file is still accepted as a legacy fallback with a warning. `rn-bootstrap` and `rn-add-screen` SKILL.md say `.workflow/DESIGN.md`.
- **`dev-flow/scripts/show_state.py` was not stack-aware.** It proposed `design-md-to-app` for an `expo-rn` project and printed *unknown phase* at `module_added` (its key was still the legacy kebab-case). The proposal is now keyed by `stack.framework` — `next` / `expo-rn` / `monorepo` / `agent`, mirroring SKILL.md §Stack-aware routing — covers `feature_complete` and `deployed`, normalizes legacy aliases, and refuses an unrecognized framework instead of guessing (the same rule dev-flow states for itself).

### Changed
- **Stripe `apiVersion` pin** (`module-add/references/module-payments.md`) — bumped `2026-07-29.dahlia` → **`2026-08-26.dahlia`**, matching stripe-node 22.6.0's generated types. Caught by the 2026-09-08 ecosystem-watch pass (`docs/vercel-changelog-watch.md`); this pin has now drifted one behind on three consecutive passes.
- `transitions`' Tier 4 note now cross-references `vgpu-shaders` §Animating once you're here (Motion 13.2.0's `vgpuEffect`) — an implementation detail inside Tier 4, not a change to when Tier 4 is justified.

### Added
- **`eve-concepts.md` §Workflow tools** — `defineWorkflowTool` from `eve/tools` (shipped eve 0.48.0–0.52.0): a static tool whose own executor is a durable Workflow run, for waiting on a person/webhook/timer without holding compute. Distinct from `experimental_workflow`, which lets the model itself write orchestration code. `eve-docs-coverage.md` gained the new `/docs/tools/workflows` page as a row.
- **`vgpu-shaders/SKILL.md` §Animating once you're here** — `motion/vgpu`'s `vgpuEffect` (Motion 13.2.0+) as the way to drive vgpu uniforms/scene params with Motion's spring/gesture engine, for a project already on both Motion and vgpu. Does not change the Tier 3→4 decision.
- **`heroicons-animated` → `animated-icons`** — the skill now covers **two** registries, so the old
  name had started to lie. [heroicons-animated](https://www.heroicons-animated.com/) (316 icons) and
  [hugeicons-animated](https://hugeicons-animated.com/) (165 icons), both MIT, both on `motion`, both
  copied into your repo as source. One skill rather than two: the procedure is identical and only the
  base URL changes. **Choose the registry by the project's static icon set, never by the icon count** —
  an animated Heroicon beside Hugeicons outlines is a different drawing. One structural difference to
  know: hugeicons items import a shared `lib/use-icon-animation.ts` (`registry:lib`), so an item is
  **not** always one file, and a hand-copied icon without the hook will not compile.
- **`lib/utils.ts` now defaults to [`cn`](https://github.com/shadcn-ui/cn)** instead of the
  `clsx` + `tailwind-merge` helper `shadcn init` writes — a compiled drop-in, same API, zero
  dependencies. Safe ahead of the CLI for a reason that was checked rather than assumed: the
  components `shadcn add` writes import **only `cn` from `@/lib/utils`**, so what sits behind that
  name was always ours to pick. Existing projects migrate with
  `npx shadcn@latest migrate cn --dry-run`; rolling back is two commands. Tailwind v4 only, and
  never `cn build` in a published component library.

### Added
- **`spec-review`** (core · operative) — reviews a **diff** on the two axes a dev-flow project can check that a generic reviewer cannot: **Spec** (does it implement what `.workflow/PRD.md` + `tasks.md` asked?) and **Standards** (does it obey the golden rules, the declared `meta.json#stack`, the discipline skills, with a Fowler smell baseline as the floor). Two parallel sub-agents, reported side by side and **never merged** — a change can follow every convention while building the wrong feature, or build the right feature ignoring the declared stack, and one verdict lets either hide behind the other. **44 skills** now: **6 core** · 15 web · 2 agent · 16 mobile · 3 monorepo · 2 refactor.

  Adapted from [Matt Pocock's `code-review`](https://github.com/mattpocock/skills) (MIT) and [the essay behind it](https://www.aihero.dev/skills-code-review); the two-axis split, the parallel sub-agents and the smell baseline are his. What is ours is the half his skill has to search for — his step 2 is four fallbacks ending in "ask the user where the spec is", while a dev-flow project keeps the spec and the standards **at known paths**. Deliberately **not** named `code-review`: Claude Code ships that name, and his own write-up lists the collision as a known problem — the same class of clash `setup-deploy` had with a third-party skill.

  It is **not a fourth pre-deploy gate**. The three gates read the finished artefact; this reads the *change*, so dev-flow proposes it at `page_generated` / `module_added` — a spec finding is cheapest while the branch is still open.
- **`framework: "agent"`** — the agent-only topology is now a first-class contract value: an eve agent at the repo root, no web app, surfaces elsewhere (Slack, email, GitHub, Linear). Closes a decision left open when `kody-eve-template` showed that `agent: "eve"` does **not** imply a monorepo. `eve-agent` gains **layout C** (agent at root, no `apps/*`, no `packages/types`) and, uniquely in this topology, **bumps `phase` to `scaffolded`** — it is the bootstrap skill there, the counterpart of `design-md-to-app` and `rn-bootstrap`, and without it the phase would never advance. Subsequent capability additions still only append to `history`.

  The 17 skills that guard on `framework` were **deliberately left untouched**: refusing an agent-only project is the correct answer, not a bug — `forms`, `data-fetching`, `shadscan` and the rest exist to build and audit a frontend that isn't there. `dev-flow` gains a routing row and is told not to apologise for their absence.
- **§Topology policy** in `dev-flow` — when an agent is in scope, three shapes are possible and the *project* picks: ① single web app (default), ② monorepo, ③ agent-only. Starting at ① costs nothing (`monorepo-bootstrap` promotes later; moving `agent/` is a directory move). Choosing ② up front costs workspace overhead for a second app that may never ship — so ask what the second consumer is; no answer means ①.
- **`shadscan`** — the **third pre-deploy gate**, completing the trio: legal (`compliance-audit`) · cost (`vercel-doctor`) · **UI quality + accessibility**. It wraps the third-party [`@shadscan/cli`](https://www.shadscan.com/) (MIT, deterministic and read-only — no app start, no file edits, no LLM, no source upload, no secrets), which scores a React/shadcn app out of 100 across Foundation · Interaction · States · Accessibility · Forms · Production Polish with file:line evidence. The skill reads the `--json` report's `agentHandoff` block — where each actionable carries a **`disposition`** (`fix` / `decide` / `verify`), a **`confidence`**, and machine-checkable acceptance criteria — applies the confirmed defects, surfaces the `decide` items to the user as product questions, and routes the real corrections to the owning skill (`forms`, `transitions`, `data-fetching`, `design-md-to-app`, `composition-patterns-guide`). Records `meta.json#shadscan`; no phase bump; web only.

  This closes a real hole: two of shadscan's rules are **verbatim our own contract** — `animations-respect-reduced-motion` is the core rule of `transitions`, and the label / error-association rules are what `forms` prescribes. Nothing in the suite could previously verify that the UI we prescribe actually got built.

  Verified by running it end-to-end on a live Next 16 + eve project: **64/100 (grade D)**, 59 rules, 17 actionables (9 `fix` · 7 `verify` · 4 `decide`) → **70/100 (C)** after the fixes. The skill carries the anti-gaming rule shadscan states itself: **never add unused infrastructure to raise the score**.

  **The run rewrote the skill's trust guidance.** Opening all 9 `fix` items in the source left **2 standing**. shadscan invents nothing — every string it quoted was really there — but it cannot follow a prop across a component boundary, climb to a wrapper, see through a Base UI `render` prop, or know that a subtree renders to WebGL instead of the DOM. A focus ring "suppressed" by an input whose wrapper carries `focus-within:ring`, a pending state lifted into the parent, a `fallback={null}` inside a `<Canvas>`, a label missing from a design-system primitive: all reported, none defects. **The better-composed the codebase, the more false positives.** So the skill now calibrates trust by *the kind of question a rule asks* — *does this file exist?* is reliable, *is this component semantically complete?* is not — and states plainly that `confidence` is not a precision estimate (`high` items split 2 true / 3 false).

  It also records the case where **the fix is real and the rule stays red**: a header that hid its whole `<nav>` below `sm` left one route unreachable on a phone; keeping the links visible fixed it, but `mobile-nav-present` still fails because the detector wants a trigger+panel pair. Ship the fix, leave the rule red, write down why — adding a hamburger for two links is the anti-gaming rule in textbook form.
- **`docs/knowledge-index.md` §Pre-deploy gates** — the two gates that wrap an external CLI now have a row each, because their flags and report schema move independently of us.
- **`vercel-deploy`** (web · operative) — the skill that actually ships a Next.js 16 project to Vercel, and the only one that sets `phase = "deployed"` for web. Preview → smoke → **staged** production (`vercel --prod --skip-domain`) → `vercel promote` → domains + DNS, plus a rollback runbook. It ships; it does not configure — `vercel.json`, region and the env-var matrix stay with `module-add deploy`, and a missing config routes there instead of being improvised. Grounded in the CLI docs' own *Preferred production commands*, which name `--skip-domain` / `promote` / `rollback` over `vercel alias`. References: `deploy-checklist.md`, `domains-dns.md`, `rollback-runbook.md`. **43 skills** now: 5 core · **15 web** · 2 agent · 16 mobile · 3 monorepo · 2 refactor.
- **Lint check #8** — bare-backtick skill references at a routing marker (`→ \`x\``, `invoke \`x\``, `use \`x\``…) are now verified against the skills on disk. Check #6 only saw `` `<name>/SKILL.md` `` paths, which is why a dangling reference could survive in prose.

### Changed
- The suite is **44 skills** (6 core · 15 web). `dev-flow` proposes all three gates at `feature_complete` and re-runs them in the `deployed` loop, and proposes `spec-review` earlier — when a chunk of work lands.
- `transitions` non-negotiable #2 (`prefers-reduced-motion`) is now labelled **machine-checkable**, pointing at the rule that verifies it; `forms` audit mode notes that passing *our conventions* does not imply passing the *user-facing outcome* (a textbook TanStack Form with no `<FormLabel>` passes the greps and fails the gate).
- **Contract**: the `feature_complete` row now routes web to `vercel-deploy`. Re-vendored to all 30 skills.
- Every reference to `setup-deploy` — a skill that was routed to but **never existed**, in `dev-flow`, `module-add`, `vercel-doctor` and the contract — now points at `vercel-deploy`. The name changed for two reasons: `setup-deploy` collides with an unrelated third-party skill of the same name that installs to `~/.claude/skills/`, and the *setup* has always belonged to `module-add deploy`.
- `dev-flow`'s README routing block no longer marks `feature_complete` / `deployed` as "(expo-rn only)" — web and the eve agent have owners there too.

## [1.0.0] — 2026-08-02

First **versioned** release. The suite has been in daily use since 2026-04-25 (183 commits); this is the point it became installable as a Claude Code plugin and started carrying a version.

### Added
- **Plugin distribution** — `.claude-plugin/plugin.json` + `marketplace.json`. Install with `/plugin marketplace add lukedj78/dev-flow` then `/plugin install dev-flow@dev-flow`, with automatic updates. `install.sh` remains for Codex / Copilot / Gemini / Cursor. The manifest's skill allowlist is **generated** from the canonical taxonomy (`scripts/build_plugin_manifest.py`), so it cannot drift.
- **41 skills** across six families: 5 core · 13 web · 2 agent · 16 mobile · 3 monorepo · 2 refactor.
- **`compliance-audit`** — GDPR + EU AI Act audit of an existing project (10-point risk register) with safe auto-remediation and flagged legal decisions; proposed as a pre-deploy gate.
- **`vercel-doctor`** — cost/performance pre-deploy gate; detects costly Vercel patterns and routes the real fixes to the owning skill.
- **`transitions`** — one tokenized motion system (cheapest-tier-first, `prefers-reduced-motion` always).
- **`heroicons-animated`** — Motion-animated Heroicons from the shadcn registry, with the accessibility guard the upstream components lack.
- **Knowledge principle (contract rule zero)** — the skills are a doc-grounded second brain: never invent an API; every "use library X" ships the *how* from X's official docs; `[VERIFY]` on fast-moving surfaces; periodic re-verification.
- **Golden rules** — ① all code (identifiers, constants, comments) is written in English, independent of the conversation language; ② every frontend ships i18n from day one (web `next-intl`, mobile the RN stack), minimum locales `en` + `it`, no hardcoded user-facing copy.
- **11 doc-grounded how-tos** for the library defaults: next-intl, i18n on RN, nuqs, Vercel Blob storage, Vercel deploy config, TanStack Query on RN, Zustand, Maestro, tw-animate-css, AI Elements, and maps (mapcn / mapcn-rn).
- **`docs/knowledge-index.md`** — the map of every how-to and the upstream to re-verify.
- **Obsidian vault** — committed `.obsidian/` config (markdown links, ignore filters, graph coloured by family) + `docs/OBSIDIAN.md`.
- **eve**: the `eve add` / `eve registry` integrations CLI, Slack `onMessage`/`onEvent` hooks and `ctx.cancel()`/`ctx.reset()` session controls, plus the audit-hook and read-vs-egress governance recipes.

### Changed
- **Contract**: `feature_complete` and `deployed` are now **cross-stack** (they were mobile-only, which meant the web compliance gate never fired); `monorepo_initialized` added to the phase enum.
- **Taxonomy**: family/role now come from one explicit map with **fail-fast** on an unclassified skill — the previous silent `"web"` fallback had misfiled `rn-upgrade` as web and flattened core/agent/monorepo/refactor skills into web.
- `nuqs` adopted as the canonical URL-state library for the "URL state" rung in `data-fetching` and `state-discipline`.
- Monorepo web token sharing now detects **Tailwind v4** (CSS-first `@theme`) with the v3 JS preset as fallback.
- Repository root cleaned: 41 skill folders + 6 support directories + 5 files (test artefacts, stray bundles and an empty `apps/` removed — all previously untracked).

### Fixed
Upstream verification caught guidance that was wrong in practice:
- `vercel-doctor` was documented without its required **path argument** — the real invocation is `npx -y vercel-doctor@latest .`.
- `mapcn` installs as a single namespaced item (`shadcn add @mapcn/map`), not per-component registry URLs; **`mapcn-rn` has no `init` command** at all.
- `tw-animate-css` ships **no shimmer utility** — the skeleton recipe now uses `animate-pulse` or a Tier-1 keyframe.
- FlashList v2 **deprecated** `estimatedItemSize` — "No longer used" (it was documented as required); Reanimated `Layout` → `LinearTransition`; Gesture Handler relabelled v3.
- React Native Firebase namespaced API → modular (rnfirebase.io: the namespaced API "is being completely removed in v22"; current major is v26); `@testing-library/jest-native` dropped (matchers are built into RNTL); Supabase session store moved off SecureStore (2 KB value limit truncates sessions).
- React Email 6 unified `@react-email/components` into `react-email`; `@hookform/resolvers` bumped to ^5 for Zod 4; Stripe `apiVersion` refreshed.
- nuqs `throttleMs` is deprecated in favour of `limitUrlUpdates: debounce(…)`; i18next dropped the v3 JSON format in **v24** — `compatibilityJSON` has accepted only `'v4'` since, on 26 as on 24 (Hermes still needs the `@formatjs/intl-pluralrules` polyfill).
- Two pairs of contradictory duplicate references consolidated to a single source of truth (Maestro setup, TanStack Query setup).

[Unreleased]: https://github.com/lukedj78/dev-flow/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/lukedj78/dev-flow/releases/tag/v1.0.0
