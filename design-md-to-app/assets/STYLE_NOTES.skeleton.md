# Style notes

> Generated from `DESIGN.md`. The DESIGN.md file is the source of truth — this
> file restates qualitative rules that don't fit into theme code, so future
> agents and humans don't have to re-derive them.

## Brand summary

<!-- One paragraph: who this is for, the emotional tone, the visual identity.
     Pull from the DESIGN.md "Overview" / "Brand & Style" section. -->

## Encoded rules

These rules are already enforced by the theme / Tailwind config / component
overrides:

<!-- Bullet list. Examples:
- "headline-xl" carries `font-display`, 72px, tight letter spacing.
- Buttons use `rounded-lg` (8px) by default.
- Card surfaces are glassmorphic (`.glass` utility / `MuiCard` override).
- Hover states emit an ambient glow tinted with `--primary`.
-->

## Rules to apply manually

These rules can't be encoded mechanically — they require judgment per page or
component. Follow them when building new screens.

<!-- Examples:
- Hero backgrounds should use `.bg-hero-radial` paired with the `tertiary`
  color in the focal center, fading to `background` at the edges.
- Component groups: tight internal spacing (8px / 16px), wide external margins
  (64px / 80px) — creates "islands" of content.
- Reserve `headline-xl` for at most one element per page.
-->

## Do's

<!-- Verbatim from DESIGN.md "Do's and Don'ts" if present. -->

## Don'ts

<!-- Verbatim from DESIGN.md "Do's and Don'ts" if present. -->

## Unknown / preserved sections

Per the design.md spec, unknown sections in the source must be preserved.
Anything from `DESIGN.md` that didn't map cleanly to theme code or to one of
the buckets above is reproduced here verbatim.

<!-- Paste preserved sections here, each under its original H2 heading. -->
