{
  "name": "@dev-flow/agent-guards",
  "version": "0.1.0",
  "description": "The four mechanical defences an agent needs when its tools return text nobody at your company wrote. One file, copied into a project by dev-flow's eve-agent skill; tested here.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/guards.d.ts",
      "default": "./dist/guards.js"
    }
  },
  "types": "./dist/guards.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "keywords": [
    "agent",
    "prompt-injection",
    "untrusted-content",
    "eve",
    "guardrails"
  ]
}
