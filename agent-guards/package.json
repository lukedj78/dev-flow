{
  "name": "@dev-flow/agent-guards",
  "version": "0.1.0",
  "description": "Five mechanical defences for an agent whose tools return text nobody at your company wrote: fence untrusted results, gate writes on provenance, bound what memory accepts, refuse in the result.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "keywords": [
    "agent",
    "prompt-injection",
    "untrusted-content",
    "eve",
    "guardrails"
  ]
}
